services:
  phice:
    image: c4ffe14e/phice:latest
    container_name: phice
    build: .
    ports:
      - "5000:5000"
    volumes:
      - "./config.toml:/src/phice/config.toml:ro"
    restart: unless-stopped
